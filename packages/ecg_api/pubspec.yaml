name: ecg_api
description: Shared API abstractions for ECG Pro.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.4

dependencies:
  ecg_core:
    path: ../ecg_core
  http: ^1.5.0
  http_parser: ^4.1.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.15
