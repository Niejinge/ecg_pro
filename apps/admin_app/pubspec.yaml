name: admin_app
description: "Flutter admin application for ECG Pro."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  ecg_api:
    path: ../../packages/ecg_api
  ecg_ui:
    path: ../../packages/ecg_ui
  file_picker: ^10.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
